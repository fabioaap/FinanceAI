# FinanceAI Environment Configuration

# ======================
# Authentication
# ======================

# Authentication provider: 'mock' | 'supabase' | 'firebase'
# Default: 'mock' (for development)
VITE_AUTH_PROVIDER=mock

# Supabase Configuration (uncomment if using Supabase)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Firebase Configuration (uncomment if using Firebase)
# VITE_FIREBASE_API_KEY=your-api-key
# VITE_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id

# ======================
# Sync Engine
# ======================

# Enable/disable sync engine
# Default: 'false' (sync disabled)
VITE_SYNC_ENABLED=false

# Cloud API endpoint for sync
# VITE_SYNC_ENDPOINT=https://api.financeai.com/sync

# Sync interval in milliseconds (default: 300000 = 5 minutes)
VITE_SYNC_INTERVAL=300000

# Conflict resolution strategy
# Options: 'local-wins' | 'remote-wins' | 'latest-wins' | 'manual'
# Default: 'latest-wins'
VITE_SYNC_CONFLICT_STRATEGY=latest-wins

# ======================
# Application
# ======================

# Application mode
NODE_ENV=development

# API Base URL (if using external API)
# VITE_API_BASE_URL=https://api.financeai.com

# Feature Flags
# VITE_FEATURE_PDF_PARSER=true
# VITE_FEATURE_AI_CATEGORIZATION=false
